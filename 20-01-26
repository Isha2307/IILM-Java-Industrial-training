ENCAPSULATION
Code 1
//Encapsulation means wrapping up the data (function, Methods,Variable,Object)
// into a single unit class and restrictions of the data access modifiers
//public private protected
//need for encapsulation 
//--data security
//--validation before the data change
//--controlled access
//--code maintainability

// 1--Getter and setter

class bankaccount{
    int bankbalance;//instance variable
    public void setbalance(int amount){
        if(amount>0){
        bankbalance=amount;}
        else{
            System.out.println("invalid amount");
        }
    }
    public int getbalance(){
        if(bankbalance>0){
            return bankbalance;
        }
        return 0;
    }
}
public class Main {
    public static void main(String[] args) {
    bankaccount ob=new bankaccount();
    ob.setbalance(2000);
        System.out.println(ob.getbalance());
    }
}
//abstraction
//abstract class-- class cant be initialised
//abstract method-- no body only the implementation is there

abstract class Vehicle{
    abstract void start();
            
        }
class Car extends Vehicle{
    void start(){
    System.out.println("car starts");
    }
}
class Bike extends Vehicle{
    void start(){
    System.out.println("bike starts");
    }
}
public class Main {
    public static void main(String[] args) {
    Vehicle v1 = new Car();
    Vehicle v2 = new Bike();
    v1.start();
    v2.start();
        
    }
}

//Local variable
//global variable -- instance
//static(defined using static keyword)refers to a particular object
//dependency-"is a" relationship


